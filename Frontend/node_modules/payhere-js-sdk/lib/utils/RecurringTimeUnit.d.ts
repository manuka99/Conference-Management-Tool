declare enum TimeUnit {
    WEEK = "Week",
    MONTH = "Month",
    YEAR = "Year"
}
export declare abstract class RecurringTimeUnit {
    private nUnits;
    private timeUnit;
    constructor(nUnits: number, timeUnit: TimeUnit);
    toString(): string;
}
export declare class Week extends RecurringTimeUnit {
    constructor(nWeeks: number);
}
export declare class Month extends RecurringTimeUnit {
    constructor(nMonths: number);
}
export declare class Year extends RecurringTimeUnit {
    constructor(nYears: number);
}
export {};
